export interface IUser {
  id?: string;
  name?: string;
  email: string;
  password: string;
}

//TODO: НИНА дописать интерфейс если сюда идет логика для юзера + проект
// дописывать через project?: что-то
// потому что для авторизации это поле необязательное
